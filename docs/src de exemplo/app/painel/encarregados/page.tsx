'use client'

import { EncarregadosContent } from './components/EncarregadosContent';

export default function EncarregadosPage() {
  return (
    <div className="h-[calc(100vh-4rem)]">
      <EncarregadosContent />
    </div>
  );
}